# Treasure_Island
A simple text-based adventure game written in Python where the player makes choices to find the hidden treasure.

## Features
- Interactive story with multiple choices.
- Different endings based on the player's decisions.
- Uses Python `if`, `elif`, and `else` statements.
